{
   "authenticationMethod": "route",
   "routes": [
      {
         "source": "^/service/runjob(.*)$",
         "target": "$1",
         "destination": "srv_api",
         "authenticationType": "xsuaa"
      }
   ]
}
